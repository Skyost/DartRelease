name: release
description: A small utility that allows to easily update your pubspec and your Github tags on new releases.
version: 0.1.2
homepage: https://github.com/Skyost/DartRelease
repository: https://github.com/Skyost/DartRelease
issue_tracker: https://github.com/Skyost/DartRelease/issues
funding:
  - https://ko-fi.com/Skyost
  - https://paypal.me/Skyost
topics:
  - tool
  - cli
  - release
  - changelog
executables:
  release:

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  args: ^2.7.0
  conventional_commit: ^0.6.1
  dotenv: ^4.2.0
  http: ^1.4.0
  intl: ^0.20.2
  liquify: ^1.3.0
  meta: ^1.17.0
  pub_semver: ^2.2.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.2

dev_dependencies:
  flutter_lints: ^6.0.0

release:
  changelog:
    header: '# 📰 `release` utility changelog'
